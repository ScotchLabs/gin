<h1>Listing shows</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Abbrev</th>
    <th>Slot</th>
    <th>Imageurl</th>
    <th>Ticketstatus</th>
  </tr>
<%
trclass = "class='zebra'"
@shows.each do |show|
  if trclass=="class='zebra'"
    trclass = ""
  else
    trclass = "class='zebra'"
  end
%>
  <tr <%= trclass %>>
    <td><%=h show.name %></td>
    <td><%=h show.abbrev %></td>
    <td><%=h show.slot %></td>
    <td><%=h show.imageurl %></td>
    <td><%=h show.ticketstatus %></td>
    <% if can? :read, Ticketsection, Ticketrez %><td><%= link_to 'Show', show %></td><% end %>
    <% if can? :update, Show %><td><%= link_to 'Edit', edit_show_path(show) %></td><% end %>
    <% if can? :destroy, Show %><td><%= link_to 'Destroy', show, :confirm => 'Are you sure?', :method => :delete %></td><% end %>
  </tr>
<% end %>
</table>

<br />

<% if can? :create, Show %><%= link_to 'New show', new_show_path %><% end %>
