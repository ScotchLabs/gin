<% ast = "<span style=\"color:red;font-variant:italics;\">*</span>" %>
<h1>New update</h1>

<% form_for(@update) do |f| %>
  <%= f.error_messages %>

  <p style="color:red;font-variant:italics;">
	* Required
  </p>

<p>
    <%= f.label :name %><%= ast %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :anchor %><%= ast %><br />
    <%= f.text_field :anchor %> (you will not be able to change this later)
  </p>
  <p>
    <%= f.label :expiredate, "Expiration date" %><%= ast %><br />
    <%= f.datetime_select :expiredate %>
  </p>
  <p>
	You can now put link templates in news articles and content articles!<br />
	Rule of Thumb: [[<code>type</code>+<code>anchor</code>|<code>text</code>]]<br />
	<ul><li><code>type</code> can be either a (for outside links), c (inside link to content), i (image tag), p (inside link to pane), or m (outside link to email)</li>
	<li><code>anchor</code> should either match the anchor of the content or pane, or it should be a valid email or http address</li>
	<li>the "|<code>text</code>" is optional, and will override the link text. in the case of image tags, this overrides the image's title and alt-text.</li></ul>
	Note: <code>anchor</code> may contain any of the following characters: 0-9 a-z A-Z . : / _ - ~ % &amp; # = @<br />
	<code>text</code> may contain any of the following characters: 0-9 a-z A-Z space ' "<br />
	<span style='color:red;'>Make sure any images are placed in /public/image/ BEFORE you publish this!</span>
  </p>
  <p>
    <%= f.label :article %><br />
    <%= f.text_area :article, :cols => 162 %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', updates_path %>