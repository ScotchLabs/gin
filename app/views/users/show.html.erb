<p>
  <b>Name:</b>
  <%=h @user.name %>
</p>

<p>
  <b>Email:</b>
  <%=h @user.email %>
</p>

<p>
  <b>Roles:</b>
  <% @user.roles.each {|r| %>
    <%= link_to r.name, r %><% if @user.roles.index(r) < @user.roles.length-1 %>,<% end %>
  <% } %>
</p>

<% if User.find(session[:user_id]).hasaccess("users", "update") %><%= link_to 'Edit', edit_user_path(@user) %> |<% end %>
<%= link_to 'Back', users_path %>